const CONSTANTS = Object.freeze({
  MSG: Object.freeze({
    500: "something broke",
    404: "no resouce",
    403: "wrong access",
  }),
  RULE: Object.freeze({
    COMMENT_PAGE_LIMIT: 10,
  }),
});

export default CONSTANTS;
