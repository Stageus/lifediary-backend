const CONSTANTS = Object.freeze({
  MSG: Object.freeze({
    500: "something broke",
    404: "no resouce",
    403: "wrong access",
    400: "unvalid value",
  }),
  RULE: Object.freeze({
    COMMENT_PAGE_LIMIT: 10,
  }),
});

export default CONSTANTS;
